# TODO

[x] number and boolean tokens
[ ] separate symbol tokenization (which should make the token of length 1 and advance the lexer) from simple token creation (which copies start pointer, sets type and sets length to 0);
[ ] parser

