# ~/.config/hypr/hyprland.conf

$src = ~/.config/hypr/src

source = $src/themes/mocha.conf
source = $src/environment.conf
monitor = eDP-1, 1920x1200@60.00300, 0x0, 1.5 # see https://wiki.hyprland.org/Configuring/Monitors/
monitor = HDMI-A-1, preferred, auto-right, 1.5 # see https://wiki.hyprland.org/Configuring/Monitors/
# monitor = , preferred, auto, 1, mirror, eDP-1

source = $src/general.conf
source = $src/decoration.conf
source = $src/animations.conf
source = $src/input.conf
source = $src/scrolling.conf
source = $src/bindings.conf
source = $src/gestures.conf
source = $src/plugin.conf



source = $src/autostart.conf

xwayland {
        force_zero_scaling = true
}

group {
    # see https://wiki.hypr.land/Configuring/Variables#Group
    auto_group = true # true
    insert_after_current = true # true
    focus_removed_window = true # true
    drag_into_group = 1 # true
    merge_groups_on_groupbar = true
    merge_floated_into_tiled_on_groupbar = false
    group_on_movetoworkspace = false
}

misc {
    disable_hyprland_logo = true
    disable_splash_rendering = false
    disable_scale_notification = false
    col.splash = $text
    font_family = FiraCode Nerd Font
    vrr = 1
    enable_swallow = true # false
    swallow_regex = ^(kitty)$
    # more
}

# MORE TO DO HERE!
#

cursor {
    
}


layerrule = blur on, match:namespace waybar
layerrule = ignore_alpha 0.0, match:namespace waybar
layerrule = blur on, match:namespace notifications
layerrule = ignore_alpha 0.0, match:namespace notifications
layerrule = blur on, match:namespace wofi
layerrule = ignore_alpha 0.0, match:namespace wofi
windowrule {
        name = opacity 1.0 override 1.0 override
        match:class = siril
}
# For all categories, see https://wiki.hyprland.org/Configuring/Variables/








#master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
#    new_is_master = true
#}


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
# device:epic-mouse-v1 {
#     sensitivity = -0.5
# }

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# windowrulev2 = nomaximizerequest, class:.* # You'll probably like this.
